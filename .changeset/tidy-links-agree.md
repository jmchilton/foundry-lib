---
'@galaxy-foundry/reference-contract': patch
---

Expose `referenceShapeIssue()` so instance schemas can enforce each reference kind's declared
wiki-link or path shape with the same wiki-link grammar used by rendering and casting.
