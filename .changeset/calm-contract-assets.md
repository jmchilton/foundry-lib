---
'@galaxy-foundry/reference-contract': patch
---

Resolve the shipped reference vocabulary through its public package export when a server bundler
moves the loader away from the package data directory.
